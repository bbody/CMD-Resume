import { CONSTANTS, StyleEnum, styleState } from "../helpers/constants.js";

window.CONSTANTS = CONSTANTS;
window.StyleEnum = StyleEnum;
window.styleState = styleState;
window.defaultStyles = styleState.defaults;
